repositories{
  jcenter()
  flatDir{
      dirs 'libs'
  }
  maven { url 'dependencies' }
}

dependencies {
   compile(name:'sst-1.7.3', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
